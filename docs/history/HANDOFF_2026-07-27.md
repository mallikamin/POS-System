# HANDOFF - continue this project in this fresh session

You are the continuation of a paused session. Work through these steps in order.
Quality first. No scope drift.

## 1. Refresh context (do not skip)

- Invoke the `/refresh` skill for this project (this directory).
- **`STATE.md` is a one-screen dashboard, not a monolith.** Read it first, then
  `_state/README.md`, then the ONE topic file covering what you are about to work on.
- Frozen history is in `docs/history/`. Never treat it as current. Its `README.md` lists
  claims in those files that are **known false**.
- Newest session log: `_state/sessions/2026-07-27_0700.md`.

## 2. Absorb the operating context

- Read `ERROR_LOG.md`. Do not repeat known mistakes. Five were added on 2026-07-27.
- `_context/` holds client REFERENCE material (transcripts, proposal, menu, DNS dump, voice
  notes with transcriptions). Status does not live there, it lives in `_state/`.
- Credentials: `_context/secrets/` (gitignored) and `INFRASTRUCTURE_CREDENTIALS_REFERENCE.md`.
  Reference by path only, **never echo values**.
- `memory/server-deployment-rules.md` before any server operation.
  `memory/data-integrity.md` before any database operation - `pg_dump` first, no exceptions.
  ⚠️ Those live in `C:\Users\Malik\.claude\projects\C--Users-Malik-desktop-pos-project\memory\`,
  **not** in this repo. There is no `memory/` folder here.
- Verify infra and DNS against actual state before any deploy or credential action (zero-trust).
  **Verify DNS against the authoritative nameserver, never your local resolver.**
- Honor the global CLAUDE.md (credential safety, shell discipline, no em dashes).

## 3. Where the work actually stands

Goal (unchanged): ship the Chick Shack UK online ordering channel - storefront, POS-backed
ordering, Stripe - alongside the client's existing EposNow till.

**Printing is solved and built.** Do not reopen it. The kitchen printer is on the shop LAN
(Ethernet switch to broadband router, client-confirmed 2026-07-27 06:43), so it is reachable
from the tablet on TCP:9100. **Zero hardware to buy.** No Pi, no mini PC, no second printer.
The print fires on the Accept tap the shop makes anyway, which is why no background service
and therefore no Android Doze problem. Detail in `_state/printing.md`, full option space and
reasoning in `_state/printing-options.md`.

**Blocked on the client, not on us:** the printer's IP address and its paper width (48 columns
for 80mm, 32 for 58mm - guessing wrong silently truncates every line). Both are on the
self-test slip. Then three print routes get tested, in order, per `printing-options.md`.

### Next, in priority order

1. **Order-queue tablet view.** An order arrives, he accepts or rejects, sets the lead time,
   it prints. **This is the thing the client believes he is buying** - his words:
   *"the exact same tablet, which is connected [to a] printer. When an order comes in he
   either accepts or rejects, and he can change the lead time."* Server side is done. Adapt
   the existing KDS. Scope it to that description and resist adding to it.
2. **Wire `storefront/src/components/Checkout.tsx`** to `POST /api/v1/public/orders`.
   `place()` still fabricates a reference and creates no order.
3. **Stripe Checkout + signature-verified idempotent webhook.** Blocked on the client's
   Stripe account (OI-20). Ask before starting.
4. Only then flip `SHOP.orderingEnabled` to `true`.

### Already done, do not redo

- Migration `n0o1p2q3r4s5` is **applied and verified locally**. It had a real bug
  (`created_at` with no `server_default`) which is fixed. **Not yet applied on the server.**
- `delivery_areas` is **seeded** locally via `app/scripts/seed_chick_shack_delivery.py`
  (idempotent). **Not yet run on the server.**
- The backend test suite is **alive again after four months dead** - 317 passing. 12 failures
  remain, all pre-existing and unrelated (10 are parked QB Desktop). See OI-34.
  Run it: `docker exec pos-system-backend-1 python -m pytest -q`

## Guardrails

- No scope drift. Same goal, nothing new.
- No compromise on quality. **Never trust a "tests passing" claim without running them** -
  this repo carried one for four months.
- Verify before any load-bearing DB, infra, credential, or deploy action.
- Never echo credential or secret values anywhere.
- **`chickshackg84.com` carries the client's LIVE EMAIL** (MX to livemail.co.uk, four
  `livemail*._domainkey` DKIM CNAMEs, SPF, DMARC). Never delete or proxy those. Verify mail
  still resolves after any DNS change. `wrangler` cannot edit DNS - that needs the dashboard.
- Do NOT set `SHOP.orderingEnabled = true` until the public order endpoint and the Stripe
  webhook are live and tested end to end. A fake order confirmation is worse than no site.
- **Never let the client buy hardware without sending him the link first.** That rule has now
  stopped two wasted purchases in one week - a Bluetooth printer, then a replacement printer
  he did not need. Note how the second happened: he was repeating **our own superseded
  advice** back to us as fact. Correct bad advice loudly, or it comes back as a purchase.
- `storefront/public/print-test.html` is generated (`app/scripts/make_print_test_page.py`),
  **not hand-edited**, and **not deployed**. Deploying puts it on the client's live domain,
  which is Malik's call.
- Client-facing writing: **no em dashes**, no jargon, short sentences. He said the technical
  language lost him.
- Stage selectively when committing. **Never `git add .`** - the tree carries ~86 files of
  unrelated pre-existing doc churn on top of this work.
