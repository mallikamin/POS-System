fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
pydantic[email]==2.10.4
pydantic-settings==2.7.1
PyJWT[crypto]==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
redis==5.2.1
python-multipart==0.0.20
httpx==0.28.1
cryptography==44.0.0
reportlab==4.1.0
openpyxl==3.1.5
lxml==5.2.1  # QB Desktop QBXML processing
stripe==15.3.1  # Card payments for online orders (authorise, capture on accept)
# OCR goods receiving + the ordering advisor. Both features are opt-in and the
# system runs unchanged without an API key, but the package is imported lazily
# so it must be installed for them to be available at all. 1.x is built on
# httpx2, which coexists with the httpx pin above rather than conflicting.
anthropic==1.0.0
# Menu item and ingredient photographs: every upload is decoded and re-encoded
# (EXIF-rotated, capped at 1200 px, JPEG) before it is stored, see
# services/media_service.py. Nothing an uploader authored is ever served.
Pillow==11.3.0
